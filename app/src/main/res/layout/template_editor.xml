<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/chat_msg_input"
    android:addStatesFromChildren="true"
    android:orientation="vertical" >

    <!-- <TextView
        android:id="@+id/timing_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="2dp"
        android:gravity="center"
        android:paddingLeft="8dp"
        android:layout_marginLeft="7dp"
        android:drawablePadding="5dp"
        android:textColor="#1a9df0"
        android:textSize="15sp"
        android:drawableLeft="@drawable/timing_sms_icon"
        android:visibility="gone" /> -->

    <com.cong.chenchong.widget.EditTextBase
        android:id="@+id/dd_et_input"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="@color/transparent"
        android:gravity="top|left"
        android:hint="@string/input_text_hint"
        android:inputType="textCapSentences|textAutoCorrect|textMultiLine|textShortMessage"
        android:maxLength="700"
        android:maxLines="6"
        android:minHeight="75dp"
        android:minLines="1"
        android:paddingBottom="4dp"
        android:paddingLeft="8dp"
        android:paddingTop="4dp"
        android:scrollHorizontally="false"
        android:scrollbars="vertical"
        android:textColor="@color/black"
        android:textColorHint="#ADB4BB"
        android:textSize="16sp" />

</LinearLayout>